version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    env_file: .env
    volumes:
      - .:/app
    networks:
      - clash_and_card_network

networks:
  clash_and_card_network:
    external: true
  